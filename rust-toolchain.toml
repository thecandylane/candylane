# Pinned toolchain for reproducible builds (ROADMAP Phase 0).
# Pinned to the version we verified green: cargo check + clippy + 13/13 tests pass.
# Bump deliberately, never float.
[toolchain]
channel    = "1.96.0"
components = ["rustfmt", "clippy"]
targets    = ["x86_64-pc-windows-msvc"]
