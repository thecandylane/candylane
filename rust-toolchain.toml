# Pinned toolchain for reproducible builds (ROADMAP Phase 0).
# Bump deliberately, never float.
[toolchain]
channel    = "1.77.0"
components = ["rustfmt", "clippy"]
targets    = ["x86_64-pc-windows-msvc"]
